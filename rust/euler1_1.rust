// Euler1 in Rust

fn euler1(n: i32) -> i32 {
    let mut retval: i32 = 0;

    for i in 1..=n {
        if i % 3 == 0 || i % 5 == 0 {
            retval += i;
        }
    }
    retval
}

fn main() {
    println!("Euler1 = {}", euler1(999));
}