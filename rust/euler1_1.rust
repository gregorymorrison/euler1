// Euler1 in Rust

fn euler1(size:int) -> int {
    let mut retval:int = 0;

    for i in range(1,size) {
        if i % 3 == 0 || i % 5 == 0 {
            retval += i;
        };
    }
    return retval;
}

fn main() {
    println( euler1(1000).to_str() );
}
